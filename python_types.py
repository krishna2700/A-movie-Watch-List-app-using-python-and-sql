# Python supports dynamic typing where variables can hold any type (int, str, list, dict, etc.)
# Type hints (e.g., def func(x: int) -> str:) are optional annotations for documentation and tools like mypy
# Common built-in types include int, float, str, bool, list, tuple, dict, set, and None
