# Python has dynamic typing where variables can hold any type: int, float, str, bool, list, dict, tuple, set, etc.
# Type hints (e.g., x: int = 5) provide optional static type annotations for better code clarity and IDE support.
# Use isinstance(obj, type) to check types at runtime, or type(obj) to get an object's type directly.
