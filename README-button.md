# Color Change Button

A simple HTML button that changes color when clicked.

## Features

- Blue button by default
- Turns red when clicked

## Usage

Open `button.html` in a web browser and click the button to see the color change.

## Implementation

- HTML `<button>` element with id `colorButton`
- CSS for blue background styling
- JavaScript click event listener that changes `backgroundColor` to red
