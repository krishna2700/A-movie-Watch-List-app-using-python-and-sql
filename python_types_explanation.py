# Python types define the kind of data a variable can hold (int, str, float, list, dict, etc.)
# You can check types using type() or isinstance(), and use type hints (e.g., name: str) for documentation and static analysis.
# Type hints help catch errors early and make code more readable, but Python remains dynamically typed at runtime.
