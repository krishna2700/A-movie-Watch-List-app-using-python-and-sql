"""
Python Types Explanation:
1. Python is dynamically typed - variables can hold values of any type and can change types during execution.
2. Common built-in types include: int, float, str, bool, list, dict, tuple, set, NoneType.
3. Type hints (e.g., def func(x: int) -> str) help document expected types but don't enforce them at runtime.
"""
