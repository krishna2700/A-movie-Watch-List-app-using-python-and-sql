# Movie Watch List App

This repository now contains:

- A Python + SQLite backend prototype in the project root.
- A React frontend app in `frontend/` created with Vite.

## Run the React app

```bash
cd frontend
npm install
npm run dev
```
